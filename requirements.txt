# No external dependencies required
# The script uses only Python standard library modules:
# - os
# - sys
# - subprocess
# - pathlib
# - typing

# However, the installed SDKs may require additional dependencies
# which will be installed during the SDK installation process
